# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# Umbra ZK assets — fetched from https://zk.api.umbraprivacy.com (see assets/zk/README.md)
assets/zk/*.zkey

# Claude Code local session artifacts
.claude/
claude-session.jsonl

# Internal / legal docs — never commit to public repo
/Atlas documents - (Seedless Labs_ Inc.)/
seedless-labs-limited-ng/

# Personal financial receipts (Claude API + grant submissions) — never commit
/grant-receipts/

# Local-only docs / drafts / commercial sheets (some contain PII) — never commit
*.docx
*campaign pricing*.pdf
/notes/Seedless Mainnet-beta_Submissions_*.csv
/notes/*signups*.csv

# Local resumes/CVs — keep local
resumes/

# Large local-only media
*.mp4

# Personal school scratch under notes/ — keep local, never commit to the app repo
/notes/ch*-datapack.md
/notes/ch*-fable-prompt.md
project final/

# Legal + third-party docs and personal school scratch — never commit to a PUBLIC repo
/notes/*.pdf
/notes/*mNDA*
/notes/Consolidated_References_DRAFT.md

# Personal / travel documents — never in a public repo
/Breakpoint*.pdf

# Visa / PoF + store-enrollment notes — bank details and personal docs, NEVER commit
/notes/visa-*.md
/notes/apple-*.md

# Scratch agent config — holds slots for a bot token and a bearer token
/zeroclaw/
claude-session-filtered.jsonl

# Business, legal and raise material — never in a public repo
notes/affidavit-*.md
notes/apple-submission-pack-*.md
notes/apple-enrollment-pack-*.md
notes/bnb-scope-*.md
notes/call-note-*.html
notes/call-note-*.pdf
notes/Seedless-GTM-Plan-*.md
notes/Seedless-BNB-Proposal-*.pdf
notes/visa-*.md
assets/pitch-deck-v6.html
assets/deck-v6-export/
